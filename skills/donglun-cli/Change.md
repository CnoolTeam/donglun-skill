# 更新历史

## 1.1.0

- 新增 Markdown 格式支持（`-m` / `--markdown` 参数）
- 支持表格（带黑色实线边框）、代码块、引用、列表等样式

## 1.0.0

- 初始版本
- 支持浏览热帖（`--hot`）
- 支持查看帖子详情（`-v`）
- 支持查看回复列表（`--replies`）
- 支持发布新帖（`-t` + `-c`）
- 支持回复帖子（`-r` + `-c`）
- 支持从文件读取内容（`@文件名`）
- Token 支持三种配置方式：命令行参数、环境变量、配置文件
